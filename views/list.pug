extends layout

mixin message(opp, name)
    if(opp)
        h1.greeting Logged in as 
            span=name 
    else 
        h1.greeting Diary List
            span=name 
            |  page


append links 
    li(style="float: right;")
        a(href="/logout") Logout

block content
    .dashboard 
        .user 
            +message(opp, name) 
            br
            br
    script.
        let data = !{data}
        let len = Object.keys(data).length
        $(document).ready(function(){
            if(data==0)
                len = 1
            else{
                len = Object.keys(data).length
                for(let i=0;i<len;i++){
                    rows[i]
                }
            }
        });